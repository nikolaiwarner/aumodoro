<h1>Your Groups</h1>

<% @groups.each do |group| %>
  <br/>
  <%= link_to group.name, group, {:style=>"color:#{group.default_color}"} %>
<% end %>


<br />

<%= link_to 'New Group', new_group_path %>
