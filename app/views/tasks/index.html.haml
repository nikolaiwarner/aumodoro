- title "Tasks"


%ul
  - @tasks.each do |task|
    %li
      = check_box_tag :complete, task.complete, task.complete
      = task.name
      = task.project.name if task.project
      
%br/
= link_to 'New Task', new_task_path



= text_field_tag :name
= submit_tag 'Save'
